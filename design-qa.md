# TradeReplay 单图首屏 V4 设计 QA

日期：2026-07-21

分支：`codex/website-redesign-v2`

## 对照目标与证据

- 用户否决的旧首屏：`/var/folders/9l/b086cnm1603_z9w1thdt61hh0000gn/T/codex-clipboard-3d49f8b5-5d49-406a-9d92-e877eb1ac7bc.png`
- 用户确认的新视觉稿：`/Users/charlie/.codex/generated_images/019f849b-cf86-7553-bc9c-8a539645cb96/exec-a3e10c07-1010-4609-85c8-d363ba7bcd78.png`
- 最终桌面实现：`qa/single-hero-v4-approved.png`
- 最终移动端实现：`qa/single-hero-v4-mobile-final.png`
- 最终中文实现：`qa/single-hero-v4-zh.png`
- 产品画面细节裁切：`qa/single-hero-v4-product-crop.jpg`
- 产品原始画面：`assets/app-current-replay-dark.png`

桌面视口为 1440 × 1024，移动端视口为 390 × 844。英文首页、顶部状态、未展开菜单、系统标签默认选中。

## 完整视图比较

新视觉稿与最终桌面实现已在同一次图像输入中并排检查。实现保留了新稿的两列比例、三行主张、单一完整产品界面、主下载按钮、文字式 Pro 入口、三项信任信息、四项横向价值条，以及下一章节在视口底部露出的节奏。

最终实现与视觉稿的主要结构一致：首屏高 701px，价值条从 774px 开始；产品区标题在约 1020px 开始出现。桌面端只有一张首屏产品图，宽 785px、高 491px，四条边完整可见，没有旋转、裁切或重叠。

## 细节比较

原始 2880 × 1800 产品画面与实现中的产品区域裁切已在同一次图像输入中检查。实现使用原始真实截图按比例缩放，完整保留顶部导航、左右工具栏、订单面板、主图表、下方交易区和底部状态栏；没有重新绘制、拉伸、遮罩或替代 UI。

## 迭代历史

1. **P1：两张产品图相互覆盖且第二张被视口截断。** 旧首屏同时放置复盘图和统计图，并使用旋转叠放。已删除首屏统计图、删除旋转和绝对定位，只保留一张按原始比例完整显示的复盘界面。统计诊断仍在后面的独立产品标签中完整展示。
2. **P2：首屏高度过长，流程与产品画面不能在同一视口内闭合。** 已把桌面 hero 收敛为 701px，将四项价值移到 129px 的独立横条；首屏和价值条在 1024px 视口内完整结束。
3. **P2：新稿底部是连续浅色章节，初版实现却出现大面积空黑区。** 已将产品展示区改成白色编辑式表面，并把标题改为 `Practice with purpose.`，消除突兀的黑色断层。
4. **P2：移动端产品图底边低于首个 844px 视口 21px。** 已把文案与产品之间的移动端间距从 42px 收紧到 18px；最终图片顶边 628px、底边 841px，完整进入首个移动视口且无横向溢出。
5. **P2：截图说明被放进深色图片框，容易被误认为产品的一部分。** 已把说明移到截图外部，产品本身的完整边界更明确。

## 最终质量检查

- **字体与层级：** 使用项目既有 Inter / SF Pro / 中文系统字体栈；导航、眉题、三行主标题、正文和价值条的字重与行距接近视觉稿，没有截断或异常换行。
- **间距与布局：** 桌面使用 1320px 内容宽度和克制的两列网格；产品图、文案和价值条没有覆盖。移动端变成单列，`scrollWidth === clientWidth`。
- **颜色与视觉变量：** 保持 `#f7f9fd` 浅色画布、深海军蓝正文和品牌蓝行动点；没有渐变、装饰 blob、玻璃拟态或 CSS 绘图。
- **图片质量：** 首屏直接使用真实 v1.50.20 隔离样例截图，按原始 1.6:1 比例缩放，所有图片加载成功。
- **文案与内容：** 英文主张、正文、按钮和四项价值与确认稿一致；中文同步改写并通过切换检查。
- **图标：** 确认稿价值条里的示意图标未作为硬性品牌资产提供。实现采用一致的 01–04 编号，不伪造图标；这是可接受的 P3 差异。
- **交互：** 语言切换、移动菜单、产品三标签点击、左右方向键切换、FAQ 和锚点导航保持可用。
- **可访问性：** 单一 H1、图片 alt、语义按钮与链接、tab/tabpanel 关联、焦点状态均保留。
- **控制台：** 页面控制台日志为空。浏览器工具自身的 Statsig 网络超时不来自官网代码。
- **链接：** 首页 21 个唯一目标、截图页 12 个唯一目标；本地文件存在，同页锚点都有对应 id。

## 剩余 P3

- 如果未来加入正式授权的同风格图标，可把价值条的 01–04 换成图标；当前编号更克制，也不会影响理解。
- Whop listing URL 仍需在上架后替换 `data-href-en`。

final result: passed
